@model System.Collections.Generic.IEnumerable<User>

@{ 
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Главная</title>
</head>
<body>
    @Html.ActionLink("Авторизация", "AuthorizationPage")

    <select multiple>
        @foreach(var item in Model)
        {
            <option>@item</option>
        }
    </select>
</body>
</html>

